# 🚀 Развертывание бота на облачном сервере (бесплатно)

## 📋 Варианты бесплатного размещения

### 1. Railway (РЕКОМЕНДУЕТСЯ) ⭐
- ✅ **500 часов бесплатно в месяц** (достаточно для работы 24/7)
- ✅ Простая настройка
- ✅ Автоматический деплой из GitHub
- ✅ Не засыпает

### 2. Render
- ✅ Бесплатный тариф
- ⚠️ Может засыпать после 15 минут неактивности (но бот продолжит работать)
- ✅ Простая настройка

### 3. Fly.io
- ✅ Бесплатный тариф
- ✅ Хорошая производительность
- ⚠️ Требует больше настроек

---

## 🚂 Вариант 1: Railway (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Подготовка

1. Создайте аккаунт на [Railway](https://railway.app)
2. Войдите через GitHub

### Шаг 2: Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий (или создайте новый)
4. Railway автоматически определит Python проект

### Шаг 3: Настройка переменных окружения

1. В проекте Railway откройте вкладку **"Variables"**
2. Добавьте переменную:
   - **Name**: `BOT_TOKEN`
   - **Value**: ваш токен бота (из BotFather)

### Шаг 4: Деплой

Railway автоматически запустит бота. Проверьте логи в разделе **"Deployments"**.

### Шаг 5: Проверка работы

1. Откройте Telegram
2. Отправьте команду `/startlohotron@LohotronRuletBot`
3. Бот должен ответить

**Готово!** Бот теперь работает 24/7 на сервере Railway.

---

## 🎨 Вариант 2: Render

### Шаг 1: Подготовка

1. Создайте аккаунт на [Render](https://render.com)
2. Войдите через GitHub

### Шаг 2: Создание Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `lohotron-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python LohotronBot.py`

### Шаг 3: Настройка переменных окружения

1. В разделе **"Environment"** добавьте:
   - **Key**: `BOT_TOKEN`
   - **Value**: ваш токен бота

### Шаг 4: Деплой

Нажмите **"Create Web Service"**. Render запустит бота.

**Примечание**: На бесплатном тарифе сервис может засыпать после 15 минут неактивности, но бот продолжит работать при получении сообщений.

---

## 🪂 Вариант 3: Fly.io

### Шаг 1: Установка Fly CLI

```bash
# Windows (PowerShell)
iwr https://fly.io/install.ps1 -useb | iex
```

### Шаг 2: Создание аккаунта

```bash
fly auth signup
```

### Шаг 3: Создание приложения

```bash
fly launch
```

Следуйте инструкциям. Fly.io создаст конфигурацию автоматически.

### Шаг 4: Настройка переменных

```bash
fly secrets set BOT_TOKEN=ваш_токен_здесь
```

### Шаг 5: Деплой

```bash
fly deploy
```

---

## 📝 Подготовка репозитория для деплоя

### Необходимые файлы (уже созданы):

1. ✅ `requirements.txt` - зависимости Python
2. ✅ `LohotronBot.py` - основной файл бота
3. ✅ `Procfile` - для Heroku/Railway (опционально)
4. ✅ `runtime.txt` - версия Python (опционально)

### Важно: НЕ добавляйте в Git:

- ❌ `lohotron.db` - база данных (создастся на сервере)
- ❌ `.env` файлы с токенами
- ❌ `__pycache__/` - кэш Python

### Создайте `.gitignore`:

```
__pycache__/
*.pyc
*.db
.env
*.log
```

---

## 🔧 Настройка для разных платформ

### Railway
- Автоматически определяет Python
- Использует `requirements.txt`
- Переменные окружения через веб-интерфейс

### Render
- Требует указать Build и Start команды
- Переменные окружения через веб-интерфейс

### Fly.io
- Требует `fly.toml` (создается автоматически)
- Переменные через `fly secrets`

---

## ✅ Проверка работы после деплоя

1. Откройте логи в панели управления платформы
2. Убедитесь, что нет ошибок
3. Отправьте команду боту в Telegram
4. Проверьте ответ

---

## 🆘 Решение проблем

### Бот не отвечает

1. Проверьте логи на платформе
2. Убедитесь, что переменная `BOT_TOKEN` установлена
3. Проверьте, что бот запущен (статус в панели)

### Ошибки при деплое

1. Проверьте, что все зависимости в `requirements.txt`
2. Убедитесь, что Python версии 3.8+
3. Проверьте логи сборки

### База данных

База данных `lohotron.db` создастся автоматически на сервере. 
**Важно**: На бесплатных тарифах данные могут быть потеряны при перезапуске сервиса.

---

## 💡 Рекомендации

1. **Используйте Railway** - самый простой и надежный вариант
2. **Делайте резервные копии** базы данных (если важно)
3. **Мониторьте логи** первые несколько дней
4. **Используйте GitHub** для автоматического деплоя

---

## 📚 Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Render Documentation](https://render.com/docs)
- [Fly.io Documentation](https://fly.io/docs)

---

**Удачи с развертыванием! 🚀**

